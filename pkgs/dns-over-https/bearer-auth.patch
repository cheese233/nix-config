diff --git a/doh-client/client.go b/doh-client/client.go
index f6e1581..8d0376a 100644
--- a/doh-client/client.go
+++ b/doh-client/client.go
@@ -217,13 +217,21 @@ func NewClient(conf *config.Config) (c *Client, err error) {
 
 		s := selector.NewNginxWRRSelector(time.Duration(c.conf.Other.Timeout) * time.Second)
 		for _, u := range c.conf.Upstream.UpstreamGoogle {
-			if err := s.Add(u.URL, selector.Google, u.Weight); err != nil {
+			bearerToken := u.BearerToken
+			if bearerToken == "" {
+				bearerToken = c.conf.Other.BearerToken
+			}
+			if err := s.Add(u.URL, selector.Google, u.Weight, bearerToken); err != nil {
 				return nil, err
 			}
 		}
 
 		for _, u := range c.conf.Upstream.UpstreamIETF {
-			if err := s.Add(u.URL, selector.IETF, u.Weight); err != nil {
+			bearerToken := u.BearerToken
+			if bearerToken == "" {
+				bearerToken = c.conf.Other.BearerToken
+			}
+			if err := s.Add(u.URL, selector.IETF, u.Weight, bearerToken); err != nil {
 				return nil, err
 			}
 		}
@@ -237,13 +245,21 @@ func NewClient(conf *config.Config) (c *Client, err error) {
 
 		s := selector.NewLVSWRRSelector(time.Duration(c.conf.Other.Timeout) * time.Second)
 		for _, u := range c.conf.Upstream.UpstreamGoogle {
-			if err := s.Add(u.URL, selector.Google, u.Weight); err != nil {
+			bearerToken := u.BearerToken
+			if bearerToken == "" {
+				bearerToken = c.conf.Other.BearerToken
+			}
+			if err := s.Add(u.URL, selector.Google, u.Weight, bearerToken); err != nil {
 				return nil, err
 			}
 		}
 
 		for _, u := range c.conf.Upstream.UpstreamIETF {
-			if err := s.Add(u.URL, selector.IETF, u.Weight); err != nil {
+			bearerToken := u.BearerToken
+			if bearerToken == "" {
+				bearerToken = c.conf.Other.BearerToken
+			}
+			if err := s.Add(u.URL, selector.IETF, u.Weight, bearerToken); err != nil {
 				return nil, err
 			}
 		}
@@ -258,13 +274,21 @@ func NewClient(conf *config.Config) (c *Client, err error) {
 		// if selector is invalid or random, use random selector, or should we stop program and let user knows he is wrong?
 		s := selector.NewRandomSelector()
 		for _, u := range c.conf.Upstream.UpstreamGoogle {
-			if err := s.Add(u.URL, selector.Google); err != nil {
+			bearerToken := u.BearerToken
+			if bearerToken == "" {
+				bearerToken = c.conf.Other.BearerToken
+			}
+			if err := s.Add(u.URL, selector.Google, bearerToken); err != nil {
 				return nil, err
 			}
 		}
 
 		for _, u := range c.conf.Upstream.UpstreamIETF {
-			if err := s.Add(u.URL, selector.IETF); err != nil {
+			bearerToken := u.BearerToken
+			if bearerToken == "" {
+				bearerToken = c.conf.Other.BearerToken
+			}
+			if err := s.Add(u.URL, selector.IETF, bearerToken); err != nil {
 				return nil, err
 			}
 		}
diff --git a/doh-client/config/config.go b/doh-client/config/config.go
index 78207a3..a334e62 100644
--- a/doh-client/config/config.go
+++ b/doh-client/config/config.go
@@ -36,8 +36,9 @@ const (
 )
 
 type upstreamDetail struct {
-	URL    string `toml:"url"`
-	Weight int32  `toml:"weight"`
+	URL         string `toml:"url"`
+	Weight      int32  `toml:"weight"`
+	BearerToken string `toml:"bearer_token"`
 }
 
 type upstream struct {
@@ -58,6 +59,7 @@ type others struct {
 	Verbose               bool     `toml:"verbose"`
 	DebugHTTPHeaders      []string `toml:"debug_http_headers"`
 	TLSInsecureSkipVerify bool     `toml:"insecure_tls_skip_verify"`
+	BearerToken           string   `toml:"bearer_token"`
 }
 
 type Config struct {
diff --git a/doh-client/doh-client.conf b/doh-client/doh-client.conf
index 20b2f41..cd204eb 100644
--- a/doh-client/doh-client.conf
+++ b/doh-client/doh-client.conf
@@ -17,6 +17,11 @@ listen = [
 upstream_selector = "random"
 
 # weight should in (0, 100], if upstream_selector is random, weight will be ignored
+#
+# bearer_token (optional): Bearer token for Authorization header.
+# If set, "Authorization: Bearer <token>" will be added to every request
+# to this upstream.  The global bearer_token under [others] can also be
+# used as a fallback; the per-upstream value takes precedence.
 
 ## Google's resolver, good ECS, good DNSSEC
 #[[upstream.upstream_ietf]]
@@ -140,6 +145,11 @@ no_user_agent = false
 # Enable logging
 verbose = false
 
+# Optional bearer token for Authorization header.
+# This value is used as a fallback when no per-upstream bearer_token is set.
+# If set, "Authorization: Bearer <token>" will be added to every upstream request.
+# bearer_token = ""
+
 # insecure_tls_skip_verification will disable necessary TLS security verification.
 # This option is designed for testing or development purposes,
 # turning on this option on public Internet may cause your connection
diff --git a/doh-client/google.go b/doh-client/google.go
index 90b75df..ffb08bd 100644
--- a/doh-client/google.go
+++ b/doh-client/google.go
@@ -95,6 +95,9 @@ func (c *Client) generateRequestGoogle(ctx context.Context, w dns.ResponseWriter
 	} else {
 		req.Header.Set("User-Agent", "")
 	}
+	if upstream.BearerToken != "" {
+		req.Header.Set("Authorization", "Bearer "+upstream.BearerToken)
+	}
 	req = req.WithContext(ctx)
 
 	c.httpClientMux.RLock()
diff --git a/doh-client/ietf.go b/doh-client/ietf.go
index 69ccbe4..8e7a988 100644
--- a/doh-client/ietf.go
+++ b/doh-client/ietf.go
@@ -134,6 +134,9 @@ func (c *Client) generateRequestIETF(ctx context.Context, w dns.ResponseWriter,
 	} else {
 		req.Header.Set("User-Agent", "")
 	}
+	if upstream.BearerToken != "" {
+		req.Header.Set("Authorization", "Bearer "+upstream.BearerToken)
+	}
 	req = req.WithContext(ctx)
 	c.httpClientMux.RLock()
 	resp, err := c.httpClient.Do(req)
diff --git a/doh-client/selector/lvsWRRSelector.go b/doh-client/selector/lvsWRRSelector.go
index 940bbd9..80779c0 100644
--- a/doh-client/selector/lvsWRRSelector.go
+++ b/doh-client/selector/lvsWRRSelector.go
@@ -24,7 +24,7 @@ func NewLVSWRRSelector(timeout time.Duration) *LVSWRRSelector {
 	}
 }
 
-func (ls *LVSWRRSelector) Add(url string, upstreamType UpstreamType, weight int32) (err error) {
+func (ls *LVSWRRSelector) Add(url string, upstreamType UpstreamType, weight int32, bearerToken string) (err error) {
 	if weight < 1 {
 		return errors.New("weight is 1")
 	}
@@ -35,6 +35,7 @@ func (ls *LVSWRRSelector) Add(url string, upstreamType UpstreamType, weight int3
 			Type:            Google,
 			URL:             url,
 			RequestType:     "application/dns-json",
+			BearerToken:     bearerToken,
 			weight:          weight,
 			effectiveWeight: weight,
 		})
@@ -44,6 +45,7 @@ func (ls *LVSWRRSelector) Add(url string, upstreamType UpstreamType, weight int3
 			Type:            IETF,
 			URL:             url,
 			RequestType:     "application/dns-message",
+			BearerToken:     bearerToken,
 			weight:          weight,
 			effectiveWeight: weight,
 		})
diff --git a/doh-client/selector/nginxWRRSelector.go b/doh-client/selector/nginxWRRSelector.go
index fafb5b6..e4663be 100644
--- a/doh-client/selector/nginxWRRSelector.go
+++ b/doh-client/selector/nginxWRRSelector.go
@@ -21,13 +21,14 @@ func NewNginxWRRSelector(timeout time.Duration) *NginxWRRSelector {
 	}
 }
 
-func (ws *NginxWRRSelector) Add(url string, upstreamType UpstreamType, weight int32) (err error) {
+func (ws *NginxWRRSelector) Add(url string, upstreamType UpstreamType, weight int32, bearerToken string) (err error) {
 	switch upstreamType {
 	case Google:
 		ws.upstreams = append(ws.upstreams, &Upstream{
 			Type:            Google,
 			URL:             url,
 			RequestType:     "application/dns-json",
+			BearerToken:     bearerToken,
 			weight:          weight,
 			effectiveWeight: weight,
 		})
@@ -37,6 +38,7 @@ func (ws *NginxWRRSelector) Add(url string, upstreamType UpstreamType, weight in
 			Type:            IETF,
 			URL:             url,
 			RequestType:     "application/dns-message",
+			BearerToken:     bearerToken,
 			weight:          weight,
 			effectiveWeight: weight,
 		})
diff --git a/doh-client/selector/randomSelector.go b/doh-client/selector/randomSelector.go
index 8a3ca7b..e398b26 100644
--- a/doh-client/selector/randomSelector.go
+++ b/doh-client/selector/randomSelector.go
@@ -18,13 +18,14 @@ func NewRandomSelector() *RandomSelector {
 	return new(RandomSelector)
 }
 
-func (rs *RandomSelector) Add(url string, upstreamType UpstreamType) (err error) {
+func (rs *RandomSelector) Add(url string, upstreamType UpstreamType, bearerToken string) (err error) {
 	switch upstreamType {
 	case Google:
 		rs.upstreams = append(rs.upstreams, &Upstream{
 			Type:        Google,
 			URL:         url,
 			RequestType: "application/dns-json",
+			BearerToken: bearerToken,
 		})
 
 	case IETF:
@@ -32,6 +33,7 @@ func (rs *RandomSelector) Add(url string, upstreamType UpstreamType) (err error)
 			Type:        IETF,
 			URL:         url,
 			RequestType: "application/dns-message",
+			BearerToken: bearerToken,
 		})
 
 	default:
diff --git a/doh-client/selector/upstream.go b/doh-client/selector/upstream.go
index f01eaba..36ce184 100644
--- a/doh-client/selector/upstream.go
+++ b/doh-client/selector/upstream.go
@@ -18,6 +18,7 @@ type Upstream struct {
 	Type            UpstreamType
 	URL             string
 	RequestType     string
+	BearerToken     string
 	weight          int32
 	effectiveWeight int32
 	currentWeight   int32
