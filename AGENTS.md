**CRITICAL RULE**: Always inspect the actual options and configuration definitions of NixOS modules first (never blindly guess or assume option names). Verify them by checking the local configuration files or pulling the actual module source code from the official GitHub repository (NixOS/nixpkgs).
